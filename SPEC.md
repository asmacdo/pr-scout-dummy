# greet CLI spec

## Usage

    greet <name> [--shout]

## Arguments

- `name` (required): The name to greet.
- `--shout` (optional): Print the greeting in uppercase.

## Behavior

Prints "Hello, <name>!" to stdout. If `--shout` is provided, the greeting is printed in uppercase.
