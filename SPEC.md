# greet CLI spec

## Usage

    greet <name> [--shout]

## Arguments

- `name` (required): The name to greet.
- `--shout` (optional): Print the greeting in lowercase.

## Behavior

Prints "Hello, <name>!" to stdout. If `--shout` is provided, the greeting is printed in lowercase.
